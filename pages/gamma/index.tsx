import { NextPage } from "next";
import Gamma from "../../components/gamma/home";
import Layout from "../../components/gamma/Layout";


const Page: NextPage = () => {
  return (
    <Layout>
        <Gamma />
    </Layout>
  );
};

export default Page;
