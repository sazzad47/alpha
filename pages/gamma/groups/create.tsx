import { NextPage } from "next";
import Create from "../../../components/gamma/groups/create";
import Layout from "../../../components/gamma/Layout";


const Page: NextPage = () => {
  return (
    <Layout>
        <Create />
    </Layout>
  );
};

export default Page;
