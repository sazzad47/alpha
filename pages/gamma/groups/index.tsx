import { NextPage } from "next";
import Groups from "../../../components/gamma/group";
import Layout from "../../../components/gamma/Layout";


const Page: NextPage = () => {
  return (
    <Layout>
        <Groups />
    </Layout>
  );
};

export default Page;
