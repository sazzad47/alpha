import { NextPage } from "next";
import Pages from "../../../components/gamma/pages";
import Layout from "../../../components/gamma/Layout";


const Page: NextPage = () => {
  return (
    <Layout>
        <Pages />
    </Layout>
  );
};

export default Page;
