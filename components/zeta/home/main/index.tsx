import React from 'react'
import { Grid } from '@mui/material'

const Main = () => {
  return (
    <Grid className='h-[90vh] w-[calc(100%-15rem)] overflow-y-auto flex justify-center flex-col items-center'>
      {/* <Grid className="h-[120vh]">
         {Array.from({length:100}, (_, i)=> (
          <Grid key={i} className="bg-[blue] w-[10rem] h-[5rem] m-4">
         
          </Grid>
         ))}
      </Grid>
       Main */}
  </Grid>
  )
}

export default Main